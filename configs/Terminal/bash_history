/Volumes/RSASecurIDUtils411/TokenImporter ; exit;
tail -f system.log
cd twisted-intro/
python twisted-deferred/deferred-simulator.py
bash -c "clear && ssh -p 22 -o UserKnownHostsFile=/dev/null -o LogLevel=quiet -o StrictHostKeyChecking=no -i ~/.docker/machine/machines/dev/id_rsa docker@192.168.99.100 -t docker exec -i -t hello-world-nginx sh"
bash -c "clear && DOCKER_HOST=tcp://192.168.99.100:2376 DOCKER_CERT_PATH=/Users/ome/.docker/machine/machines/dev DOCKER_TLS_VERIFY=1 $SHELL"
git pull git@gitlab.okla.seagate.com:PoolHouse/ddump_decoder.git
git clone git@gitlab.okla.seagate.com:PoolHouse/ddump_decoder.git
docker images
boot2docker -v up
docker images
docker shell init
docker images
docker ps
docker images
ifconfig
ping 10.127.252.217
ping 192.168.59.3
export DOCKER_HOST="10.127.252.217:2376"
boot2docker ip
ps -aef | grep docker
docker images
bash -c "clear && ssh -p 22 -o UserKnownHostsFile=/dev/null -o LogLevel=quiet -o StrictHostKeyChecking=no -i ~/.docker/machine/machines/dev/id_rsa docker@192.168.99.100 -t docker exec -i -t graphite sh"
uname
brew install tcpdump
tcpdump
tcpdump --help
tcpdump -i eth0
sudo tcpdump -i eth0
sudo tcpdump -i en0
timelimit
timelimit -t 10 while [ 1 -eq 1 ]; do echo hi; sleep 1; done
timelimit -t 10
timelimit -t 10 echo hiu
timelimit -t 10 tcpdump -i en0
timelimit -t 1 sudo tcpdump -i en0
timelimit -t 2 ping 8.8.8.8
timelimit -t 5 sudo tcpdump -i en0
timelimit -t 3 -T 6 sudo tcpdump -i en0
timelimit -s 3  sudo tcpdump -i en0
timelimit -t 3 -s 4 sudo tcpdump -i en0
tcpdump -D
tcpdump -c 10 -n -i en0
sudo tcpdump -c 10 -n -i en0
sudo tcpdump -c 10 -i en0
sudo tcpdump -c 10 -i en0 udp
tcpdump -i en0 udp
sudp tcpdump -i en0 udp
sudo tcpdump -i en0 udp
sudo tcpdump -i en0 udp | grep -i ssdp
python ssdp.py server 127.0.0.1
ifconfig
python ssdp.py server 192.168.1.125
git clone git@gitlab.okla.seagate.com:PoolHouse/ddump_decoder.git
cd ddump_decoder
vagrant Vagrantfile
vagrant init
vagrant up
vagrant global-status
vagrant ssh e
vagrant ssh
vagrant ssh e
cp vijavaguest-5.5.jar ~/Desktop/
git status
ssh jenkins@jenkins.connectloud.com
ssh cl@jenkins.connectloud.com
defaults write com.apple.finder AppleShowAllFiles YES
cd ~/Library/Application\ Support/MobileSync/Backup/
df -h
df -h ./
du -h ./
